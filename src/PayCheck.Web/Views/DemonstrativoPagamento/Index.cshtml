@model IEnumerable<DemonstrativoPagamentoViewModel>

@{
    ViewData["Title"] = "Index";

    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Begin Page Content -->
<div class="container-fluid">
    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Demonstrativo de Pagamento</h1>
    </div>

    <table class="table">

        <thead>

            <tr>
                @*
                <th class="hidden-xs col-md-2 col-lg-1">SBB</th>
                <th class="hidden-xs col-md-2 col-lg-1">RP</th>
                <th class="col-xs-4 col-sm-6 col-md-4 col-lg-4">Nome</th>
                <th class="hidden-xs hidden-sm hidden-md col-lg-3 text-center">Nascimento</th>
                <th class="col-xs-8 col-sm-6 col-md-4 col-lg-3 text-center">Ações</th>
                <th>
                @Html.DisplayNameFor(model => model.Guid)
                </th>
                *@

                <th class="col-xs-3 col-sm-3 col-md-2 col-lg-2">
                    @Html.DisplayNameFor(model => model.Competencia)
                </th>
                <th class="col-xs-3 col-sm-3 col-md-2 col-lg-2">
                    @Html.DisplayNameFor(model => model.NumeroMatricula)
                </th>
                <th class="hidden-xs hidden-sm col-md-4 col-lg-4">
                    @Html.DisplayNameFor(model => model.RazaoSocialEmpregador)
                </th>
                <th class="col-xs-6 col-sm-6 col-md-4 col-lg-4">
                    @Html.DisplayNameFor(model => model.NomeColaborador)
                </th>
                <th>

                </th>
            </tr>
        </thead>

        <tbody>
            @if (Model != null)
            {
                @foreach (var item in Model)
                {
                    <tr>
                        @*                <td>
                @Html.DisplayFor(modelItem => item.Guid)
                </td>*@
                        <td>
                            @item.Competencia
                        </td>
                        <td>
                            @item.NumeroMatricula
                        </td>
                        <td>
                            @item.RazaoSocialEmpregador
                        </td>
                        <td>
                            @item.NomeColaborador
                        </td>
                        <td>
                            @*                    @Html.ActionLink("Edit", "Edit", new { id=item.Guid }) |

                    @Html.ActionLink("Details", "Details", new { id=item.Guid }) |

                    @Html.ActionLink("Delete", "Delete", new { id=item.Guid })*@

                            @Html.ActionLink("Detalhes", "Details", new { guid = item.Guid }, new { @class = "btn btn-success btn-xs" })

                            @*<a href="@Url.Action("your action", "your controller", new { id = item.Id }" class="btn btn-success btn-xs">Delete</a>*@
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4" class="text-center">
                        Nenhum registro encontrado.
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>