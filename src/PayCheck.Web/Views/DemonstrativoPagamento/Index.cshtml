@model IEnumerable<PayCheck.Web.Models.DemonstrativoPagamentoViewModel>

@{
    ViewData["Title"] = "Index";

    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Demonstrativos de Pagamento</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@

<table class="table">

    <thead>

        <tr>

            @*            <th>

            @Html.DisplayNameFor(model => model.Guid)

            </th>*@

            <th>

                @Html.DisplayNameFor(model => model.Competencia)

            </th>

            <th></th>

        </tr>

    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @*                <td>
            @Html.DisplayFor(modelItem => item.Guid)
            </td>*@
                <td>
                    @Convert.ToInt32(item.AnoCompetencia).ToString("0000") / @Convert.ToInt32(item.MesCompetencia).ToString("00")
                </td>
                <td>
                    @*                    @Html.ActionLink("Edit", "Edit", new { id=item.Guid }) |

                @Html.ActionLink("Details", "Details", new { id=item.Guid }) |

                @Html.ActionLink("Delete", "Delete", new { id=item.Guid })*@

                    @Html.ActionLink("Detalhes", "Details", new { guid = item.Guid }, new { @class = "btn btn-warning btn-xs" })

                    @*<a href="@Url.Action("your action", "your controller", new { id = item.Id }" class="btn btn-success btn-xs">Delete</a>*@
                </td>
            </tr>
        }
    </tbody>
</table>